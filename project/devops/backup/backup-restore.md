# Backup & Restore — InvestCalc

Если используется файл (JSON/SQLite):
- для backup достаточно скопировать файл в резерв:
`cp data.json data_backup.json`.

Для восстановления:
`cp data_backup.json data.json`.
