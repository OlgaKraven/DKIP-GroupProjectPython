# Deployment Manual — InvestCalc

## Локальный запуск
- установить зависимости;
- выполнить `uvicorn main:app --reload`.

## Запуск в Docker
- `docker compose up --build`.

## Проверка
- открыть `http://localhost:8000/docs`.
